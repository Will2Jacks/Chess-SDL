#!/bin/bash
# Change to the directory where the script is located (your project root)
cd "$(dirname "$0")"

# Change directory to src so that the working directory becomes ~/Chess-SDL/src
cd src

echo "Launching Chess SDL game..."
./Project
